const ensemble = {
  flute: [1, 3],
  trombone: [1, 5],
  clarinet: [1, 3],
  violin: [1, 3],
  bassoon: [1, 4],
  cello: [2, 5],
  horn: [2, 2],
  // piccolo: [1, 2],
  // trumpet: [1, 3],
};

function setEnsemble() {
  // get user's choice of ensemble
}

function getEnsemble() {
  return ensemble;
}

export { setEnsemble, getEnsemble };
